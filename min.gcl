// This returns the minimum of two integer. Supposedly valid.
min(x:int, y:int | z:int) {
  z := x ;
  if y < x then { z := y } else { skip } ;

  // Changing this to 3 < 2 will give unsat as output
  // Note, only INT < INT expressions work in the assert
  assert 1 < 2;
  assert 18 < 33;
  assert 2 < 3
}
